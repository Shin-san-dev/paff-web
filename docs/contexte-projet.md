# Contexte de PAFF

Ce document rassemble les repères durables à relire à chaque nouvelle tâche. `AGENTS.md` demande cette lecture au démarrage. Maintenir ce document lorsque le produit ou la manière de travailler change.

## Produit et vocabulaire

PAFF est un jeu de cartes et de stratégie privé développé pour un groupe d’amis. L’interface est en français, avec des tons sombres, du bronze, du parchemin et des illustrations fantasy. Conserver un ton simple et personnel ; les textes exacts fournis par l’utilisateur priment sur la reformulation.

La première version jouable date du **11 septembre 2026** et comprend **Gobelins** et **Sephosi** (clé technique `sephosi`). Le catalogue comporte aussi d’autres factions ; le journal raconte l’histoire du projet et ne doit pas être déduit automatiquement de l’état courant du catalogue. Les noms et profils d’unités sont à vérifier dans `shared/catalogue2026.ts` et les données du catalogue.

Les cinq membres initiaux sont **Nicolas, Adrien, Bru, Pierre et Quentin**. Les profils sont communautaires : pas de statistiques de parties, victoires/défaites, classement, comparaison, Elo ni progression compétitive. Ne pas confondre cette contrainte avec les compteurs déjà nécessaires au déroulement d’une partie.

## Architecture et sources

- **React 19 + TypeScript, Vite, React Router**. Routes dans `src/app/App.tsx`, pages dans `src/pages/`, fonctions et composants métier dans `src/features/`.
- **Convex + Convex Auth**. `users` identifie le compte ; `playerProfiles` porte le pseudo, l’accès, l’avatar et les badges. `gamePlayers` est l’appartenance à une partie, pas une seconde identité permanente.
- **Styles et assets** : variables dans `src/styles/global.css`, navigation dans `src/components/SiteHeader.tsx`, illustrations et logo dans `public/`. Réutiliser l’existant, sans dépendances lourdes.
- **Jeu** : état courant documenté dans `docs/regles-implementees.md`. Les comparaisons de règles datées sont dans `docs/differences-regles-*.md`.
- **Journal et profils** : `docs/journal-profils.md`, `src/features/journal/entries.ts`, `shared/playerBadges.ts`, `shared/playerAvatars.ts`, `convex/players.ts`.

## Décisions d’interface

- Journal public `/journal`, dans un groupe de navigation distinct « À propos de PAFF », séparé de Cartes / Mes decks / Lobby.
- Profils `/players/:userId` réservés aux membres actifs. Avatar, pseudo, badges et **nombre de decks total et par faction** (précision utilisateur du 13 septembre 2026) ; pas de lien de retour au lobby dans le contenu du profil. Les compteurs sont calculés depuis les decks existants, sans publier leurs noms ou leur contenu et sans statistiques de parties.
- Présentation compacte des profils : petit avatar à gauche du nom, contenu près du haut, decks et badges visibles ensemble sans défilement sur les formats usuels. Le changement d’avatar s’ouvre dans une petite fenêtre modale, sans rallonger la page. Ne pas réintroduire un grand avatar central ni de grandes marges supérieures.
- Chaque membre peut changer son propre avatar : **un choix par faction, seulement Gobelins et Sephosi pour l’instant**. Gobelins utilise le shaman, Sephosi l’ange protecteur ; Gobelins est le choix par défaut. Les anciens avatars sont ramenés à ces deux choix à l’affichage, en conservant leur faction lorsqu’elle est reconnue. La mutation détermine le propriétaire depuis la session et ne permet pas de modifier les badges.
- Le badge **« Premier jour »** (`first-version`) indique la présence dès le lancement ; il est attribué explicitement aux cinq membres initiaux. Son illustration est locale. Pas de système de déblocage automatique.
- Le logo apparaît dans la navigation et comme favicon ; ne pas le dupliquer dans le bloc principal de l’accueil.
- Les futures préférences de faction/carte et les decks publics ne doivent pas apparaître comme des sections vides.

## Retours de partie du 15 septembre 2026

- Référence courante : `PAFF 2026 (2).pdf`, 9 pages, reçue le 15 septembre. Trois profils révisés : Bande de Gobelins 2 dés ; Archers Gobelins 1 R / 2 dés ; Epéistes Sephosiens 3 DT. Valeurs versionnées dans `shared/catalogue2026.ts`, comparaison dans `docs/differences-regles-2026-09-15.md`.
- Déploiement initial : jusqu’à 21 points **sans minimum**, réserve égale au reste du deck (33 points maximum). La consigne de Nicolas autorisant 18 + 15 prime sur la limite de réserve à 12 encore présente dans le PDF. Conserver les contraintes de cases, de types et de Blop.
- Compteur Recrutement : +/− interdits au tour 1 côté interface et serveur, disponibles dès le tour 2. Calendrier de référence 2/3/4 ; les autres disponibilités et les effets restent manuels. Ne pas confondre ce compteur avec l’entrée d’une unité de réserve, qui peut provenir d’un ordre de faction.
- Spectateurs : partie visible dès son lancement, avec suivi des préparatifs sans révéler les choix privés, puis plateau public au déploiement et au combat. Toujours aucune place de joueur ni droit d’action.
- Plateau : flancs davantage séparés, liens d’engagement rouge vif continus, aperçu de carte survolable avec capacité consultable et accès clavier. Aucun changement des coordonnées ou des déplacements.

## Analyse d’équilibrage hors des profils joueurs

**Intégration WIP autorisée le 16 septembre :** Nicolas demande de commiter, fusionner `codex/audit-equilibrage` dans `main` et pousser, sans relancer les vérifications complètes. Cette intégration conserve les campagnes et leurs limites connues ; elle ne valide pas les règles encore à corriger et ne reprend pas le développement V3 en pause. Le push déclenche le déploiement Vercel configuré, dont les contrôles automatiques restent en place. Aucun déploiement Convex de production demandé dans cette étape. Reprendre ultérieurement la V3 sur une nouvelle branche dédiée depuis `main`.

**Organisation demandée par Nicolas le 16 septembre :** une campagne MCTS de 25 minutes est acceptable ; la priorité est de limiter le travail annexe, les échanges et la consommation de tokens. L’affichage de l’atelier est jugé suffisant : conserver l’interface et les relectures existantes, sans amélioration visuelle supplémentaire. Concentrer la suite sur la fidélité des règles, les capacités réellement utilisables par les bots et la qualité mesurée de leurs décisions. Les calculs de parties restent locaux sans appel à un LLM ; préparer des contrôles ciblés et un bilan automatique concis pour éviter de faire relire chaque partie à Nicolas. La discussion de méthode n’autorise pas à elle seule une nouvelle campagne ou un déploiement.

Nicolas a demandé le 15 septembre une démarche progressive : audit des règles et des 20 unités Gobelins/Sephosi, puis méthodologie et simulations permettant de distinguer les effets des decks de la qualité des stratégies. Le premier lot est une analyse théorique hors ligne, documentée dans [l’analyse des unités](equilibrage/analyse-unites.md) et [l’audit des règles](equilibrage/audit-regles.md). Les calculs se régénèrent avec `node scripts/analyse-equilibrage.mjs` (Node 24) à partir du catalogue et du tableau des touches du projet. Ils décrivent une attaque conditionnelle, pas des parties ni des performances de joueurs. Aucun changement de règle ou de coût n’en découle automatiquement.

**Référence de règles actuelle : [`paff-2026-09-15-adrien-03`](equilibrage/regles-consolidees.md).** Les trois salves sont archivées dans `docs/equilibrage/retours-adrien-*.md`. Les dix-neuf questions et les douze compléments ont reçu leurs réponses ; ne pas reprendre les anciennes listes d’attente comme questions ouvertes.

Les précisions finales priment : tir interdit le tour du recrutement ; **Tir en mouvement coûte un Mouvement et un Tir** ; Vallardi donne son ordre dès l’arrivée et le conserve jusqu’à la fin du tour de sa mort ; Fureur divine dépense points/PS sans sélection Recrutement commune supplémentaire. Trollitude au début du combat, passage des charges définitif, destruction simultanée des deux armées = égalité. Seules les relances du tableau sont supprimées ; surnombre sur tout le réseau d’engagements, allocation par attaquant, un dé relancé au plus une fois. Invokation résout les dés et dégâts avant les conséquences, sacrifie les autres Shamans disponibles sans annuler rétroactivement le bonus et ne répète pas les conséquences sur 6. Tir concentré : une zone, artillerie incluse. Pause-déjeuner : au plus un sacrifice / +1 R par Troll et par ordre. Événements exclus.

**Recrutement adopté :** stock conservé, +3 points aux tours **2/4/5**, librement dépensable via au plus trois sélections communes. **Déblocage des sélections distinct : première dès le tour 2, deuxième dès le tour 3, troisième dès le tour 4**, rappel de Nicolas d’après une modification du fichier de règles du 14 septembre. Les sélections inutilisées restent disponibles ; les utilisations cumulées ne doivent pas dépasser 1 au tour 2, 2 au tour 3, 3 à partir du tour 4. Les points/PS ne permettent pas d’anticiper ce déblocage. Le calendrier 2/3/4 figure déjà dans le texte du catalogue mais manque au contrôle du simulateur ; correction consignée, pas encore implémentée. Ne pas remplacer pour autant le calendrier des points 2/4/5 ni ajouter un Recrutement commun à Fureur divine.

**Suite confirmée, puis mise en pause par Nicolas :** nommer les prochains bots **V3**, viser la couverture de chaque unité, capacité et ordre, et les comparer aux **V2** sur les mêmes règles corrigées, avec contrôles automatisés et interface conservée. Le développement et les campagnes restent en pause à sa demande pendant qu’il consulte le créateur sur trois interactions : surnombre avec les charges ultérieures, doublement des dés supplémentaires, poursuite de Ligne Verte après la mort du Danzereu. Les nouvelles précisions reçues pendant cette pause sont consignées sans lancer leur implémentation.

**Lot 2 terminé :** consolidation, petit moteur hors ligne et 44 situations contrôlées, révision ciblée des conclusions. La pause demandée pendant les réponses est terminée. Moteur dans `analysis/simulation/`, [couverture et limites](equilibrage/situations-verifiees.md), [méthode des campagnes](equilibrage/methode-simulations.md). `npm run test:simulation` exécute les cas ; les types stricts de l’analyse sont intégrés à `npm run check`. Les 400 attaques simples ne sont pas régénérées : leur référence numérique `adrien-01` reste valide pour leur périmètre.

**Lot 3 — calibration :** après la discussion sur l’apprentissage, Nicolas a autorisé l’étape suivante. Après la remarque de Nicolas sur les trois decks Gobelins face à un seul Sephosi, le protocole est corrigé : **trois decks par faction**, neuf confrontations croisées et un contrôle miroir par deck. Les Sephosi comprennent référence, infanterie et tir. Trois robots à priorités explicites, sans RL, jouent ces six decks fixes ; `npm run simulate` produit **792 parties, 792 rejeux identiques et 396 paires inversées concordantes**. Voir [la campagne, ses résultats et ses limites](equilibrage/campagne-calibration.md). L’orchestration complète existe pour ce périmètre restreint. Plusieurs unités complexes sont exclues et les bots ignorent certains choix pourtant légaux ; ne pas présenter leur domination Gobelins comme une preuve d’équilibrage. Les copies de certains ordres de faction et les conventions recensées au lot 2 restent à traiter avant une couverture générale. La suite prioritaire est l’amélioration des pilotes et leur validation indépendante, pas un ajustement de coûts.

**Essai MCTS — 16 septembre :** Nicolas confirme le déploiement alterné unité par unité, initiative en premier, et demande d’essayer une recherche MCTS avant le RL. Le nouveau pilote adapte chaque pose au plateau public et explore les ordres jusqu’au contrôle de fin de tour. Les cartes cachées adverses et les futurs jets ne sont pas consultés. Les charges restent proposées par le pilote commun. Même moteur de règles, six decks et sélections de départ : aucun ajustement de coût ou de capacité. Protocole séparé : 72 confrontations comparables aux 72 anciennes parties Contrôle/Contrôle, 12 miroirs et 24 duels à deck identique MCTS/Contrôle. L’atelier conserve **20 relectures détaillées**, choisies avant les résultats, et permet de revenir à la référence. Méthode, limites, résultats et vérification de développement : [campagne MCTS](equilibrage/campagne-mcts.md). Ne pas présenter un taux de victoires plus proche de 50 % comme une preuve que les pilotes jouent mieux ; examiner aussi les duels à deck égal et les décisions visibles.

Résultats de l’essai MCTS : 66 victoires Gobelins / 4 Sephosi / 2 égalités sur 72 confrontations, contre 66 / 2 / 4 à référence comparable ; durée moyenne 7,11 contre 4,92 tours. À deck identique, MCTS gagne 18 duels contre l’ancien pilote, en perd deux et fait quatre égalités ; les deux défaites concernent Gobelins à trois Skrans. Le pilote progresse sur ce panel sans résoudre le bilan extrême entre factions. 108 rejeux validés, 36 paires inversées concordantes, 327 tests réussis. Les 20 exemples sont servis et vérifiés sur Convex **développement** et le lecteur local ; aucun déploiement de production.

**Défaut identifié ensuite dans `mcts-022` :** le pilote Sephosi, qui a l’initiative, termine ses ordres dès le début du tour 1 malgré trois ordres et des actions légales disponibles ; les Gobelins jouent ensuite seuls. Le lecteur omet ce passage. Le PDF p. 2 et Nicolas imposent trois ordres alternés ; le passage définitif validé par Adrien concernait les charges, pas l’abandon des ordres de base. Corriger cette option et rendre les passages visibles avant une nouvelle campagne ; les tests/rejeux réussis ne prouvent pas cette conformité. Diagnostic consigné dans la campagne MCTS, sans modification du moteur ni recalcul. Le simulateur est déjà headless, sans LLM ni apprentissage entre parties. Son traitement des réserves cachées reste incomplet et ne constitue pas un ISMCTS complet. Les bonus de charge +1 / Charge puissante +3 sont bien appliqués ; le deck Sephosi tir de `mcts-022` ne contient aucune cavalerie.

**Précisions suivantes de Nicolas — 16 septembre :** première unité de chaque joueur sur la case du milieu de sa Base Centre, **E5 au Sud / E2 au Nord** ; changement d’axe avec +1 point de mouvement ; aucun tir ni combat entre axes. Mouvement et tir sont déjà conformes dans le simulateur, mais la première pose reste trop libre et les charges entre axes sont acceptées. Ces corrections sont **consignées, pas encore implémentées** ; l’ancienne exception provisoire tout-artillerie n’est pas confirmée par cette nouvelle précision. La relecture des 84 traces MCTS du rapport retrouve 144 charges entre axes dans 66 parties. Nicolas demande également des decks couvrant Anges, Salamandre, Vallardi, Shamans, Danzereu, Blop et les autres capacités des factions. Étendre les choix réellement utilisables par les bots, pas seulement la liste des cartes ; respecter le quota d’une Unique par deck. Le panel actuel ne permet aucune conclusion globale sur l’équilibrage. [Détails, état du code et ordre de reprise](equilibrage/retours-nicolas-2026-09-16.md). Aucun changement du moteur, recalcul, synchronisation Convex ou déploiement lors de cette consolidation.

**Écarts avec le site manuel :** le calendrier de déblocage des sélections 2/3/4 reste valide. En revanche, ses textes attribuent encore trois points à chaque sélection et l’aide conserve les relances `(r)` du PDF, au lieu du stock de points séparé 2/4/5 et de la suppression de ces relances. Les nouvelles règles sont spécifiées et vérifiées hors ligne dans les cas couverts ; elles ne sont pas appliquées au site, à Convex ou aux batailles existantes par ce lot.

**Espace admin du lot 3 :** `/admin/equilibrage`, deux queries `simulations.getReport/getExample`, lecture des rapports et exemples générés côté serveur : neuf pour la référence, vingt pour MCTS. Le bilan demandé par Nicolas affiche victoires/défaites/égalités et tours moyens, globalement et par confrontation, avec les miroirs séparés des duels entre factions. Il suit le filtre ; les durées de victoire excluent défaites/égalités et les pourcentages gardent les égalités au dénominateur. Autorisation par compte actif et identifiant exact dans la variable Convex `PAFF_SIMULATION_ADMIN_USER_ID`, sans dépendre du pseudo ni du rôle. Variable absente = accès refusé. Les JSON `data/simulation/` ne sont pas importés dans le client de production ; les traces complètes restent locales et régénérables. Aucun accès aux parties réelles. La variable a été configurée pour le compte `nicolas` existant de **développement** et le backend synchronisé. La production doit utiliser son propre identifiant après vérification, lors d’une publication explicitement autorisée ; rien de ce lot n’y a été déployé. Conserver la branche `codex/audit-equilibrage` et les diffs consultables. Les résultats simulés ne deviennent jamais des performances des profils joueurs.

**Diagnostic de la domination Gobelins :** Nicolas a demandé l’analyse des parties existantes. Le [diagnostic détaillé](equilibrage/diagnostic-campagne.md) rejoue les 648 confrontations entre factions et attribue les touches, pertes, contrôles et ordres. 534 des 598 victoires Gobelins viennent de la Base Centre ; aucune partie de référence entre factions ne finit par élimination. Les Sephosi infligent davantage de dégâts effectifs, tandis que les effectifs gobelins et le contrôle alimentent l’économie d’ordres. La Bande du chef ressort comme première unité à examiner ; les relances et renforts gratuits n’expliquent pas seuls l’écart. Un essai isolé de garde de base Sephosi sur 108 configurations réduit les victoires Gobelins de 98 à 92, surtout en faveur d’égalités : ce garde-fou simple ne rééquilibre pas le panel. Outils reproductibles `scripts/diagnostiquer-simulations.mjs` et `scripts/sonder-defense-base.mjs`, sorties séparées dans `analysis/simulation/output/diagnostic/`. Aucun changement du moteur, des robots de référence, des coûts ou des résultats publiés ; aucun déploiement pour ce diagnostic. Ne pas confondre cet essai avec une validation de robots optimaux ou de toutes les capacités des factions.

## Environnements et travail local

Le dossier habituel de Nicolas est `/Users/nicolasca/Documents/workspace/paff-web`. Travailler dedans, sur une branche `codex/<sujet>`, et laisser les diffs consultables avant commit. Une nouvelle conversation doit être ouverte dans ce projet pour charger son `AGENTS.md` ; un échange sans accès au dépôt ne récupère pas automatiquement ces fichiers.

- Développement Convex configuré localement : `grateful-warthog-543`. Au contrôle du 13 septembre 2026, il contient Nicolas, Adrien et le compte de test Nicolas 2.
- Production Convex : `tough-gecko-249`. Au même contrôle, elle contient les cinq comptes initiaux. Toujours relire l’état réel avant une écriture ; ne pas supposer que les données de développement et de production sont identiques.
- Site public : <https://paff-web.vercel.app>. Vercel construit et publie l’interface depuis `main`, avec `npm run check` ; cela ne déploie pas Convex.
- Ne pas publier ni intégrer une branche implicitement. Une demande explicite d’attribution de badges autorise la mise à jour des profils ciblés, en conservant identifiants, accès, avatars choisis et autres données.

### Procédure de développement

La configuration locale vérifiée le 13 septembre 2026 contient `CONVEX_DEPLOYMENT=dev:grateful-warthog-543` et `VITE_CONVEX_URL=https://grateful-warthog-543.convex.cloud`. Relire ces deux valeurs à la reprise et vérifier qu’aucune variable de déploiement du processus ne change la cible ; ne jamais imprimer de clé ni de mot de passe.

| Besoin | Commande et cible |
| --- | --- |
| Synchroniser les fonctions, le schéma et les types pour essayer une fonctionnalité | `npx convex dev --once` : instance de développement configurée, puis arrêt de la commande. |
| Travailler avec synchronisation continue du backend | `npx convex dev` : instance de développement configurée. |
| Ouvrir l’interface locale | `npm run dev` : Vite utilise `VITE_CONVEX_URL`. |
| Lire l’état de service en développement | `npx convex run health:check` : requête sans modification de données. |
| Exécuter une fonction pour un essai en développement | `npx convex run module:fonction '…'` : vérifier si la fonction lit ou modifie les données et utiliser les identifiants réellement présents en dev. |
| Publier le backend en production lorsque demandé | `npx convex deploy` : production du projet, **pas** l’instance de développement indiquée par `.env.local`. |
| Exécuter une fonction en production lorsque demandé | `npx convex run module:fonction '…' --prod`. |

`npx convex deploy --dry-run` reste une simulation de déploiement vers la cible de `deploy` ; ce n’est ni un test hors réseau ni une synchronisation de la dev. Référence : [CLI Convex](https://docs.convex.dev/cli/overview), sections développement et déploiement ; options vérifiées dans la version installée du CLI.

Pour les badges, l’initialisation des cinq membres du lancement exige leurs cinq profils existants. Leur absence en dev ne bloque pas l’essai du profil, de l’avatar ou du rendu d’un badge sur un compte de développement existant. Suivre `docs/journal-profils.md` et garder l’attribution aux cinq comptes de production comme une étape distincte.

### Reprise entre conversations et permissions

Les repères durables vont dans `AGENTS.md` et ce document ; l’état d’une fonctionnalité va dans sa documentation dédiée. Indiquer l’environnement réellement utilisé, ce qui a été vérifié, ce qui a été publié et ce qui reste à faire. Un résultat en mémoire, en dev ou en production ne prouve pas les deux autres.

À la reprise d’une conversation déjà ouverte, relire ces fichiers depuis le disque. Tant que les modifications ne sont pas commitées puis intégrées, elles ne sont disponibles que dans la copie de travail qui les contient. La lecture des instructions au démarrage est décrite dans la [documentation Codex sur AGENTS.md](https://learn.chatgpt.com/docs/agent-configuration/agents-md).

Une restriction de permissions est indépendante de ces documents. Après un refus, distinguer un déploiement de production refusé d’un accès réseau de développement également refusé. Continuer le travail indépendant qui reste autorisé et rapporter le motif exact pour les étapes bloquées ; ne pas changer d’outil pour exécuter l’action refusée. La réussite d’un déploiement dans une ancienne session ne garantit pas les permissions d’une nouvelle session.

## Vérifications

`npm run check` : lint, tests unitaires/fonctionnels et build. `npx tsc --noEmit -p convex/tsconfig.json` : backend strict. Les tests fonctionnels utilisent les vrais handlers et écrans, avec transport et base en mémoire ; compléter par un essai réseau lorsque le lot le nécessite.
